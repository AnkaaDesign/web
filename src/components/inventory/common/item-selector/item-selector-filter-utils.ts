// Re-export the existing filter utilities from the main item list
// The item selector uses the same filter structure and logic

export { extractActiveFilters, createFilterRemover, type FilterIndicator } from "../../item/list/filter-utils";
